name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  flutter_spinkit: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  #for GetX
  get: ^4.6.6
  #for map
  flutter_map: ^5.0.0
  latlong2: ^0.9.0
  geolocator: ^10.0.0
  #for sqflite
  sqflite: ^2.3.0
  path: ^1.8.3
  sqflite_common_ffi: ^2.3.0
  #for sharedPreferences
  shared_preferences: ^2.2.2 
  #for scan
  cron: ^0.5.1
  #for firebase
  firebase_auth: ^4.17.4
  firebase_core: ^2.31.0
  cloud_firestore: ^4.8.0
  crypto: ^3.0.3  # For generating verification tokens
  
flutter:
  uses-material-design: true
  assets:
    - assets/
